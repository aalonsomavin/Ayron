Identity chip for users and the agent. Use `agent` for Ayron's avatar in chat; `name` initials for people; `status` for presence.

```jsx
<Avatar agent size={28} />
<Avatar name="Dana Lee" status="online" />
```
